#include "event.h"

const goat_callback_t event_default_callbacks[GOAT_EVENT_LAST] = {

};

int event_process(goat_context_t *context, int connection, const char *message) {
    // FIXME
    return -1;
}
